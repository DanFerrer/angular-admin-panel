import angular from 'angular';
// import { Devices } from './devices/devices';
// import { Readings } from './readings/readings';

const components = angular.module('app.components', []).name;

export default components;
